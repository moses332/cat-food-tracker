# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local Claude Code tooling (dev-only, may hold local settings)
.claude/

# Dependencies (none today, but just in case)
node_modules/

# Personal scratch notes — not part of the app
original prompt.txt
